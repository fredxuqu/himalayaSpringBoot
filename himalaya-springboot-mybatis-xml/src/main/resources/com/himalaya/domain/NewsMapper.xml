<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.himalaya.repository.HimalayaNewsDao">

	<!-- insert id="insert" parameterType="NewsDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO person(name,age) VALUES(#{name},#{age})
    </insert-->
    
    <insert id="insert" parameterType="NewsDO" >
        INSERT INTO TB_NEWS(ID,TITLE,CONTENT,AUTHOR,TYPE,PUBLISH_TIME) 
    	VALUES(#{id}, #{title}, #{content}, #{author}, #{type}, #{publishTime})
    </insert>

    <update id="update" parameterType="NewsDO">
        UPDATE TB_NEWS SET CONTENT=#{content} WHERE id =#{id}
    </update>

    <delete id="delete" parameterType="java.lang.Integer">
        DELETE FROM TB_NEWS WHERE id =#{id}
    </delete>

    <select id="getById" resultType="NewsDO">
        SELECT * FROM TB_NEWS WHERE id = #{id}
    </select>

    <select id="getAll" resultType="NewsDO">
        SELECT * FROM TB_NEWS
    </select>
</mapper>